import React from 'react';
import {Text, View} from 'react-native';
import styles from './TimeScreen.styles';

const TimeScreen = (): JSX.Element => {
  return (
    <View style={styles.mainContainer}>
      <Text>Time Screen</Text>
    </View>
  );
};

export default TimeScreen;
